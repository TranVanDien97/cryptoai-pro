const { generateSmartAlert } = require('./server/services/ai/priceAlertEngine'); const { setGeminiKey } = require('./server/services/ai/aiClient'); require('dotenv').config({path:'server/.env'}); setGeminiKey(process.env.GEMINI_API_KEY); async function test() { try { const coin = { symbol: 'ALLO', price: 0.41, entryPrice: 0.38, pnlPct: 7.8, resistance: 0.45, support: 0.35, candles: Array(30).fill({ high: 0.45, low: 0.35, close: 0.41 }), volume24h: 10000, avgVolume: 5000 }; const res = await generateSmartAlert(coin); console.log('Result:', res); } catch(e) { console.error('Error:', e); } } test();
