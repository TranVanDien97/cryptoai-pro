{
  "name": "cryptoai-pro",
  "version": "5.0.0",
  "description": "CryptoAI Pro — AI Trading Assistant with Binance Integration",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
